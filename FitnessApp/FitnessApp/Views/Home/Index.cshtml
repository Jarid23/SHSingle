@model IEnumerable<FitnessApp.Models.Models.Trainer>

@{
    ViewBag.Title = "Index";
}

<h2>Welcome</h2>

<div class="col-xs-7 divTop">
    <form class="navbar-form navbar-right" onsubmit="return false;">
        <div class="form-group">
            <button class="btn btn-primary" type="button" id="clearSearch">
                <span class="glyphicon glyphicon-ban-circle" aria-hidden="true"></span>
                Clear Search
            </button>
        </div>
        <button type="submit" class="btn btn-default" onclick="search()">Search</button>
        <div class="form-group">
            <select class="form-control" v-model="searchCategory" id="searchCategory">
                <option value="">Search By&nbsp;&nbsp;</option>
                <option value="Client">ClientName</option>
                <option value="Workout">WorkoutName</option>
            </select>
        </div>
        <div class="form-group">
            <input type="text" class="form-control" placeholder="Search Term" v-model="searchTerm" id="searchTerm">
        </div>
    </form>
</div>


<div class="col-sm-1 divMain">
</div>
<div class="col-sm-10 divMain" id="TrainersArea">
    <div class="col-xs-12 col-sm-9">
        <div class="row">
            <div class="col-xs-12">
                <h4>Sighting List</h4>
                <p>@Html.ActionLink("Add a Trainer", "AddTrainer", "User", null, new { @class = "btn btn-primary" })</p>
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Trainer Name</th>
                            <th>Start Date</th>
                            <th>Hourly Rate</th>
                            <th>Current Clientele</th>                            
                        </tr>
                    </thead>
                    <tbody>
                        @*@foreach (var trainer in Model)
                        {
                            <tr>
                                <td>@trainer.TrainerName</td>
                                <td>@trainer.StartDate</td>
                                <td>@trainer.HourlyRate</td>
                                <td>@trainer.Clients</td>                               
                                <td>
                                    @Html.ActionLink("Edit", "EditTrainer", "User", new { TrainerID = trainer.TrainerID }, null) |
                                    @Html.ActionLink("Delete", "DeleteTrainer", "Admin", new { TrainerID = trainer.TrainerID }, null)
                                </td>
                            </tr>
                        }*@
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

